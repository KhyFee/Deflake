-- Seed demo org/project for local previews (no auth.users dependency for empty bootstrap)
-- Apply only in local/dev; production uses Auth-created profiles.

-- noop marker: dashboard ships with immutable JSON demo when Supabase unset
select 1;
